<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ezypayaepssdk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:node="replace"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EzyPayActivity"></activity>

        <activity android:name=".EzypayAEPS"></activity>

        <activity android:name=".TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.finopaytech.finosdk.activity.MainTransactionActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.finopaytech.finosdk.activity.DeviceSettingActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.karumi.dexter.DexterActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.finopaytech.finosdk.fragments.BTDiscoveryFragment"
            android:theme="@style/AppTheme" />
    </application>

</manifest>